name: Hourly Ping Render Bot

on:
  schedule:
    - cron: '30 * * * *'  # Every hour
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render Scan Endpoint
        run: |
          curl -fsS --retry 3 https://crypto-rsi-alerts.onrender.com/scan || true
